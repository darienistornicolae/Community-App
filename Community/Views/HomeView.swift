import SwiftUI

struct HomeView: View {
  var body: some View {
    ZStack {
      ScrollView {

      }
      VStack {
        Spacer()
        HStack {
          Spacer()
          FloatingActionButton()
        }
      }
    }
  }
}

#Preview {
  HomeView()
}
